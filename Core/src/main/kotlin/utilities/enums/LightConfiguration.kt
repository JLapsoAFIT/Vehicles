package core.utilities.enums
/**
 * An enum class for light object configuration values
 *
 * @author Joshua A. Lapso
 * @param count The number of light objects to create
 */
enum class LightConfiguration(val count: Int){
    DEFAULT(0)
}